{% extends 'base.html.twig' %}

{% block title %}Mes Adresses - DronaMada
{% endblock %}

{% block content %}
<style>

 body {
    height: 100vh;
 }

</style>
<div class="container">
	<a href="{{path('app_account_address_add')}}" class="btn btn-info btn-sm float-right"> Ajouter une adresse</a>
	<h1>Mes Adresses</h1>
	C'est dans cet espace que vous allez pouvoir gérer toutes vos adresses.
	<br>
	<a href="{{path('app_account')}}">Retour </a>
	<hr>

	{# {{dump(app.user.addresses)}} #}
	{# si mon app.user addresse est égal a 0 alors je lui affiche un message #}
	{% if app.user.addresses|length == 0 %}
	<p class="text-center">
		Vous n'avez pas encore ajouter d'adresse dans votre compte client. Pour en ajouter une, veuillez <a
			href="{{path('app_account_address_add')}}"> cliquer ici </a>
	</p>

	{% else %}

	<div class="row">
	  {% for address in app.user.addresses %}
          {# boucle pour adresse #}

		<div class="col-md-4">
			<div class="card" style="width: 18rem; background-color:black;">
				<div class="card-body">
					<h5 class="card-title">{{address.name}}</h5>
					<p class="card-text"> 
					  {{address.address }}, <br/>
					  {{address.postal}} - {{address.city}} - {{address.country}}
					</p>
					<a href="{{path('app_account_address_edit', {'id': address.id} )}}" class="btn btn-primary">Modifier</a> | <a href="{{path('app_account_address_delete', {'id': address.id} )}}" class="btn btn-primary">Supprimer </a>
				</div>

			</div>

		</div>

	</div>

	{% endfor %}


	 {% endif %}

	{% endblock %}